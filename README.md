# Halibut
ViTech Intelligence 
$project = "LabelleSolutions/9"

gh issue create --title "Bootstrap mono-repo skeleton" `
  --body  "Create pnpm workspace, .gitignore, CODEOWNERS." `
  --label "Phase:Bootstrap,Type:Task" `
  --project $project
# …repeat for the other three issues
